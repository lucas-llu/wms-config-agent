Refine the supplied WMS/JDA MOCA document fragment without changing technical meaning. Preserve
commands, parameters, configuration keys, versions, module names, identifiers, and
source-location metadata exactly, including letter case. Never invent missing values. Do not
translate, decorate, or add characters to technical identifiers.

Return only the refined fragment.

Fragment:
{text}
