# Copy this file to .env only if your launcher loads dotenv files.
# Never commit the real value. The application reads the process environment directly.
WMS_LLM_API_KEY=replace-with-your-local-api-key
